# import logging
# import pandas as pd

# logging.basicConfig(filename='logins.log', level=logging.INFO)

# def registrar_login(usuario):
#     logging.info(f'Usuário {usuario} logado em {pd.Timestamp.now()}')

# registrar_login('usuario_exemplo')
